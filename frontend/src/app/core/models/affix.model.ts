// Affix-Label-Definitionen (für Tooltips).

export interface AffixStatLabel {
  /** Deutscher Anzeige-Name. */
  readonly de: string;
  /** Suffix nach dem Wert: '%', '' (flat) oder 'dmg'. */
  readonly suffix: string;
}
