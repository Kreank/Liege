// ITEM_SPRITES — Item-Kind → URL-Path zum Ground-Item-Sprite.
//
// Foundation-Stub (2026-05-30). Parallel-Subagent B füllt diese Map aus
// `items.ts` + den `assets/items/professional/`-Pfaden. Der
// `AssetLoaderService` registriert beim Boot alles, was hier drin steht
// als Phaser-Texture (key = `item_<kind>`).
export const ITEM_SPRITES: Readonly<Record<string, string>> = {};
